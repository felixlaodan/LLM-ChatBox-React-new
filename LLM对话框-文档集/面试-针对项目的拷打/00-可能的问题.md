# 🎯 面试官拷打指南：Next-Gen AI Chat Platform 项目深挖

> 💡 **面试官内心OS**：写了Vue重构React，说明有比较过两者的差异；写了流式返回和渲染优化，说明碰到了真实业务场景的性能瓶颈。我不会只问 API 怎么调，我会重点考察**底层原理、边界情况（Edge Cases）和技术选型的思考过程**。

---

## 一、 架构与技术选型层 (Architecture & Tech Stack)

**1. Vue -> React 迁移思考**
*   **Q:** 为什么决定把这个项目从 Vue 3 迁移到 React？仅仅是为了练习吗？在实际重构过程中，你觉得 Vue 的响应式（Proxy）和 React 的不可变数据（Immutability）在处理这种“打字机”高频更新场景时，哪种心智负担更重？
*   **Q:** 重构过程中遇到最大的难点是什么？有没有踩过什么 React Hook 的闭包陷阱（Stale Closure）？

**2. 状态管理选型**
*   **Q:** 为什么选 Zustand 而不是 Redux Toolkit 或者 React 原生的 Context API？
*   **Q:** 你的项目里提到了用 Zustand 的 `persist` 中间件做持久化。你知道它是怎么工作的吗？如果 `localStorage` 存满了（大概5MB限制）导致持久化失败，你的代码有做容错处理吗？

## 二、 核心难点：流式响应 (Streaming Response)

**3. 网络协议与底层**
*   **Q:** 你提到了 SSE（Server-Sent Events），它和 WebSocket 有什么区别？为什么大模型对话场景普遍选 SSE 而不用 WebSocket？
*   **Q:** 你是怎么实现流式请求的？为什么不用 Axios 而是直接用了 Fetch API？
*   **Q:** 如果用户在模型回复到一半的时候，突然点击了“停止生成”或者切换到了其他对话，你是怎么中断网络请求的？（期望听到 `AbortController` 及其原理）。

**4. 边界异常处理 (粘包与拆包)**
*   **Q:** 在网络不稳定的情况下，TCP 传输可能会导致“拆包”（例如一个完整的 JSON `{ "text": "hello" }` 被截断成了两半），你在解析流式 Chunk 的时候，是如何处理这种截断问题的？（期望听到对 Buffer / 残留字符串缓存的处理逻辑）。

## 三、 极致性能优化 (Performance)

**5. 高频重渲染优化**
*   **Q:** 简历上写了“利用 useRef 配合自定义 Hook 优化高频数据流更新策略”。你能具体描述一下这里的流程吗？如果不优化，直接每次拿到 token 就 `setState` 会发生什么现象？
*   **Q:** 你提到把 CPU 占用降低了 20%，你是用什么工具测量的？怎么定位到瓶颈在 React 的 Render 阶段的？

**6. 长列表与复杂渲染**
*   **Q:** Markdown 渲染通常是很耗时的（尤其是有很多代码块高亮时）。如果在流式输出期间，每次收到新 Token 都全量重新解析一遍当前消息的 Markdown，肯定会卡顿。你是怎么优化的？（考察 `React.memo` / 防抖 / 局部渲染机制）。
*   **Q:** 如果一个用户的对话历史有 1000 条消息，进入页面时会不会卡顿？你了解过虚拟列表（Virtual List）吗？如果要给这个项目加上虚拟列表，你觉得难点在哪？（引申：消息气泡高度不固定，虚拟列表怎么做）。

## 四、 现代化 UI 与工程化 (UI/UX & Engineering)

**7. 样式与组件库**
*   **Q:** 为什么使用 Tailwind CSS + Shadcn/ui，相比于以前用 Element Plus，你觉得开发体验有什么区别？
*   **Q:** 你的深浅色主题（Dark Mode）是怎么实现的？Tailwind 内部是怎么配合做主题切换的？（考察对 CSS Variables / `dark:` class 的理解）。

**8. 类型安全**
*   **Q:** 既然用了 TypeScript，为什么还要引入 Zod？TypeScript 的 Interface/Type 检查和 Zod 校验本质上有什么区别？（期望听到“编译时”与“运行时”的区别，以及针对 API 返回或表单输入的实际防护）。

---

## 🌟 绝杀题（加分项，用于定级）

*   **Q:** 如果现在产品经理要求：在模型回复很慢的时候，允许用户直接关闭浏览器，等模型回答完毕后，用户下次打开网页还能看到完整的回复。你目前的纯前端架构能实现吗？如果不能，如果让你加入后端，你会怎么设计这套架构？（考察对整体系统链路的理解：WebHook / 异步队列 / 消息中心）。
*   **Q:** 回顾这个项目，如果让你推翻重来，你会在架构设计上做哪些不同的决定？